# 开发环境（APP_ENV=dev，默认加载 .env）
APP_ENV=dev

# 数据库切换：PostgreSQL（本机实例）
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=bone
DB_PASSWORD=bone123
DB_NAME=skillhub

# LLM 审核引擎（可留空，留空时降级本地启发式引擎）
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_MODEL_NAME=deepseek-chat
LLM_API_KEY=
